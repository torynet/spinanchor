services:
  app:
    build:
      context: .
      target: dev
    ports:
      - "3000:3000"
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/packages/server/node_modules
      - /app/packages/client/node_modules
    environment:
      - NODE_ENV=development
